"""
Taramabot Grafik Üretim Modülü\nMplfinance kullanarak profesyonel mum grafikler oluşturur.\n"""\n\nimport logging\nimport os\nfrom pathlib import Path\nfrom typing import Optional\nimport pandas as pd\nimport mplfinance as mpf\n\nfrom indicators import calc_smi, calc_macd, calc_ma200, calc_rsi\nfrom config import SCREENSHOTS_DIR\n\nlogger = logging.getLogger(__name__)\n\n\nclass ChartGenerator:\n    \"\"\"mplfinance ile grafik üretici.\"\"\"\n    \n    def __init__(self, output_dir: str = SCREENSHOTS_DIR):\n        self.output_dir = output_dir\n        Path(self.output_dir).mkdir(exist_ok=True)\n    \n    def generate_smi_macd_chart(\n        self,\n        df: pd.DataFrame,\n        symbol: str,\n        period: str,\n        output_filename: Optional[str] = None\n    ) -> Optional[str]:\n        \"\"\"\n        SMI/MACD indikatörleri ile mum grafiği oluştur.\n        \n        Args:\n            df: OHLCV verileri\n            symbol: Sembol\n            period: Zaman dilimi\n            output_filename: Çıktı dosya adı (opsiyonel)\n            \n        Returns:\n            Grafik dosya yolu veya None\n        \"\"\"\n        try:\n            if df is None or df.empty:\n                logger.error(f\"Geçersiz veri: {symbol}\")\n                return None\n            \n            # İndikatörleri hesapla\n            smi, smi_ema = calc_smi(df)\n            macd, signal, hist = calc_macd(df)\n            ma200 = calc_ma200(df)\n            \n            # DataFrame'e ekle\n            df_copy = df.copy()\n            df_copy['SMI'] = smi\n            df_copy['SMI_EMA'] = smi_ema\n            df_copy['MACD'] = macd\n            df_copy['Signal'] = signal\n            df_copy['Hist'] = hist\n            df_copy['MA200'] = ma200\n            \n            # Ek grafikler (addplot)\n            addplots = []\n            \n            # MA200\n            if 'MA200' in df_copy.columns and not df_copy['MA200'].isnull().all():\n                addplots.append(\n                    mpf.make_addplot(\n                        df_copy['MA200'],\n                        color='blue',\n                        width=1.5,\n                        panel=0\n                    )\n                )\n            \n            # SMI\n            if 'SMI' in df_copy.columns and 'SMI_EMA' in df_copy.columns:\n                addplots.append(\n                    mpf.make_addplot(\n                        df_copy['SMI'],\n                        panel=2,\n                        color='green',\n                        width=1.5,\n                        ylabel='SMI'\n                    )\n                )\n                addplots.append(\n                    mpf.make_addplot(\n                        df_copy['SMI_EMA'],\n                        panel=2,\n                        color='red',\n                        width=1.5\n                    )\n                )\n            \n            # MACD\n            if 'MACD' in df_copy.columns and 'Signal' in df_copy.columns:\n                addplots.append(\n                    mpf.make_addplot(\n                        df_copy['MACD'],\n                        panel=3,\n                        color='blue',\n                        width=1.5,\n                        ylabel='MACD'\n                    )\n                )\n                addplots.append(\n                    mpf.make_addplot(\n                        df_copy['Signal'],\n                        panel=3,\n                        color='orange',\n                        width=1.5\n                    )\n                )\n                if 'Hist' in df_copy.columns:\n                    addplots.append(\n                        mpf.make_addplot(\n                            df_copy['Hist'],\n                            panel=3,\n                            type='bar',\n                            color='gray',\n                            alpha=0.5\n                        )\n                    )\n            \n            # Panel sayısını belirle\n            panels = 2 + (1 if 'SMI' in df_copy.columns else 0) + (1 if 'MACD' in df_copy.columns else 0)\n            \n            # Panel oranları\n            if panels == 2:\n                ratios = (4, 1)\n            elif panels == 3:\n                ratios = (4, 1, 2)\n            elif panels == 4:\n                ratios = (4, 1, 1.5, 1.5)\n            else:\n                ratios = (4, 1)\n            \n            # Çıktı dosya yolu\n            if output_filename is None:\n                output_filename = f\"{self.output_dir}/{symbol}_{period}_smi_macd.png\"\n            else:\n                output_filename = f\"{self.output_dir}/{output_filename}\"\n            \n            # Grafik başlığı\n            title = f\"{symbol} - {period} | SMI/MACD Analizi\"\n            \n            # Grafik oluştur\n            mpf.plot(\n                df_copy,\n                type=\"candle\",\n                volume=True,\n                title=title,\n                style=\"yahoo\",\n                savefig=output_filename,\n                addplot=addplots,\n                panel_ratios=ratios,\n                tight_layout=True,\n                figsize=(12, 8)\n            )\n            \n            logger.info(f\"Grafik oluşturuldu: {output_filename}\")\n            return output_filename\n            \n        except Exception as e:\n            logger.error(f\"Grafik oluşturma hatası ({symbol}): {str(e)}\")\n            return None\n    \n    def generate_rsi_chart(\n        self,\n        df: pd.DataFrame,\n        symbol: str,\n        period: str,\n        output_filename: Optional[str] = None\n    ) -> Optional[str]:\n        \"\"\"\n        RSI indikatörü ile mum grafiği oluştur.\n        \n        Args:\n            df: OHLCV verileri\n            symbol: Sembol\n            period: Zaman dilimi\n            output_filename: Çıktı dosya adı (opsiyonel)\n            \n        Returns:\n            Grafik dosya yolu veya None\n        \"\"\"\n        try:\n            if df is None or df.empty:\n                logger.error(f\"Geçersiz veri: {symbol}\")\n                return None\n            \n            # RSI hesapla\n            rsi_values = calc_rsi(df['close'])\n            ma200 = calc_ma200(df)\n            \n            # DataFrame'e ekle\n            df_copy = df.copy()\n            df_copy['RSI'] = rsi_values\n            df_copy['MA200'] = ma200\n            \n            # Ek grafikler\n            addplots = []\n            \n            # MA200\n            if 'MA200' in df_copy.columns and not df_copy['MA200'].isnull().all():\n                addplots.append(\n                    mpf.make_addplot(\n                        df_copy['MA200'],\n                        color='blue',\n                        width=1.5,\n                        panel=0\n                    )\n                )\n            \n            # RSI\n            if 'RSI' in df_copy.columns:\n                addplots.append(\n                    mpf.make_addplot(\n                        df_copy['RSI'],\n                        panel=2,\n                        color='purple',\n                        width=1.5,\n                        ylabel='RSI(7)'\n                    )\n                )\n                # RSI seviyeleri (30, 50, 70)\n                addplots.append(\n                    mpf.make_addplot(\n                        [30] * len(df_copy),\n                        panel=2,\n                        color='gray',\n                        width=0.5,\n                        type='line'\n                    )\n                )\n                addplots.append(\n                    mpf.make_addplot(\n                        [50] * len(df_copy),\n                        panel=2,\n                        color='gray',\n                        width=0.5,\n                        type='line'\n                    )\n                )\n                addplots.append(\n                    mpf.make_addplot(\n                        [70] * len(df_copy),\n                        panel=2,\n                        color='gray',\n                        width=0.5,\n                        type='line'\n                    )\n                )\n            \n            # Çıktı dosya yolu\n            if output_filename is None:\n                output_filename = f\"{self.output_dir}/{symbol}_{period}_rsi.png\"\n            else:\n                output_filename = f\"{self.output_dir}/{output_filename}\"\n            \n            # Grafik başlığı\n            title = f\"{symbol} - {period} | RSI Analizi\"\n            \n            # Grafik oluştur\n            mpf.plot(\n                df_copy,\n                type=\"candle\",\n                volume=True,\n                title=title,\n                style=\"yahoo\",\n                savefig=output_filename,\n                addplot=addplots,\n                panel_ratios=(4, 1, 2),\n                tight_layout=True,\n                figsize=(12, 8)\n            )\n            \n            logger.info(f\"Grafik oluşturuldu: {output_filename}\")\n            return output_filename\n            \n        except Exception as e:\n            logger.error(f\"Grafik oluşturma hatası ({symbol}): {str(e)}\")\n            return None\n\n\n# Global singleton instance\n_chart_generator_instance: Optional[ChartGenerator] = None\n\n\ndef get_chart_generator() -> ChartGenerator:\n    \"\"\"Singleton grafik üretici'yi al.\"\"\"\n    global _chart_generator_instance\n    if _chart_generator_instance is None:\n        _chart_generator_instance = ChartGenerator()\n    return _chart_generator_instance\n
