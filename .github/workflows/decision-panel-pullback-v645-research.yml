name: Karar Paneli v6.4.5 · Pullback Research · Long History
run-name: "Karar Paneli v6.4.5 · Pullback Research · Long History"

on:
  push:
    paths:
      - ".github/decision-panel-pullback-v645-bootstrap"
  workflow_dispatch:

permissions:
  actions: read
  contents: read

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: requirements-cached-scan.txt
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-cached-scan.txt
      - name: Download long-history snapshot
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          mkdir -p data /tmp/hist
          artifact_id="$(gh api "repos/${REPOSITORY}/actions/artifacts?name=historical-daily-latest&per_page=100" | jq -r '[.artifacts[] | select(.expired == false)] | sort_by(.created_at) | last | .id // empty')"
          test -n "${artifact_id}"
          gh api "repos/${REPOSITORY}/actions/artifacts/${artifact_id}/zip" > /tmp/hist.zip
          unzip -q -o /tmp/hist.zip -d /tmp/hist
          if [ -f /tmp/hist/historical_daily.sqlite ]; then
            cp /tmp/hist/historical_daily.sqlite data/historical_daily.sqlite
          elif [ -f /tmp/hist/data/historical_daily.sqlite ]; then
            cp /tmp/hist/data/historical_daily.sqlite data/historical_daily.sqlite
          else
            echo "historical_daily.sqlite bulunamadı" >&2
            exit 1
          fi
      - name: Compile Pullback research
        run: python -m py_compile decision_panel_pullback_v645_research.py decision_panel_v645_research.py
      - name: Research Pullback
        run: |
          mkdir -p reports/v645-pullback
          python decision_panel_pullback_v645_research.py \
            --db data/historical_daily.sqlite \
            --min-score 75 \
            --output reports/v645-pullback/pullback.json
      - name: Upload result
        uses: actions/upload-artifact@v4
        with:
          name: decision-panel-v645-pullback-${{ github.run_id }}
          path: reports/v645-pullback/
          if-no-files-found: error
          retention-days: 30
